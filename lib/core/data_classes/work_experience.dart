class WorkExperience {
  String title;
  String description;
  DateTime startDate;
  DateTime endDate;
  WorkExperience({
    required this.description,
    required this.title,
    required this.startDate,
    required this.endDate,
  });
}
